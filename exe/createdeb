#!/usr/bin/env ruby

require 'createdeb/cli'

log = Logger.new(STDOUT)
log.level = Logger::WARN

app = Createdeb::CLI.new(ARGV, log)
app.run!


# This is free software released into the public domain (CC0 license).
